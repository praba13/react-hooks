import React from 'react';

const Welcome = ({ firstBook, secoundBook }) => {
    //console.log(props);
    return (
        <div>Welcome to Tutorial - {firstBook} and {secoundBook} !</div>
    );
};

export default Welcome;